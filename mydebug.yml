---
- hosts: 172.31.35.149
  become: yes
  tasks:
  - name: i want to execute linux command directly
    command: hostname
    register: myresult
  
  - name: I want to display variable value
    debug: 
     msg : "{{myresult}}"
